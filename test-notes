1. npx create-react-app dashboard-ui
2. cd dashboard-ui
3. npm start  // This will start the development server and open the app in your default browser. You should see the default React app running at http://localhost:3000/.
port 3000 is the default port for React development server. If you want to change it, you can set the PORT environment variable before starting the server. 
For example, to use port 4000, you can run the following command in your terminal

Start servers
Backend:
node server.js


Fontend:
npm start
runs on http://localhost:3000

Browser:

http://localhost:3000/api/customers


to add updated the github devcodess


git push origin main --force-with-lease

git pull origin main --rebase

git push origin main



